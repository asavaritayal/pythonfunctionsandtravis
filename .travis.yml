sudo: required

language: generic

services:
  - docker

before_install:
- docker pull microsoft/azure-functions-python3.6
- docker run -d -v ".:/home/site/wwwroot" microsoft/azure-functions-python3.6  /bin/sh -c "python --version; ls;"
- docker ps -a

script:
- ls